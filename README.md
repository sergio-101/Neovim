# Personal Instruction ( Not meant to understood by dis-elite people )

- Should be in `~/.config/nvim`

- `sudo apt install fd-find ripgrep` (Telescope uses to ignore gitignore files & live grep)

- It uses terminal background, decrease transparency and change the background color (`#161616`)
